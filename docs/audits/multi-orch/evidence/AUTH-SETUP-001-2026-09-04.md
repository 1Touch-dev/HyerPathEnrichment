# `AUTH-SETUP-001` Execution Evidence — 2026-09-04

Status: `READY FOR QA RE-REVIEW`
Execution state: `COMPLETED (SCOPED FOLLOW-UP)`

## Run metadata

- Date: 2026-09-04
- Operator: Cursor agent
- Environment name: local non-production auth evidence rerun
- Environment classification: disposable local development stack; non-production only
- API revision / image / branch: `product-doors/baseline` at executed revision `b75883cbdce230b59abc8b59fae587d51db07a96`
- Backend URL: `http://127.0.0.1:8000`
- Frontend URL / port: `http://127.0.0.1:4325`
- Approved `DEC-T4-SETUP` option: reuse `backend/scripts/create_test_user.py` plus real HTTP login in a non-production environment only
- Security reviewer sign-off reference: `DEC-T4-SETUP` in `docs/audits/multi-orch/master-decision-register.md`

## Dependency checklist

- [x] `DEC-T4-SETUP` approved
- [x] Non-production environment confirmed
- [x] `GET /health` returned `200`
- [x] Approved setup mechanism available
- [x] Cleanup owner named
- [x] Cookie-jar files remain untracked

## Case ledger

| Case ID | Result | Notes | Evidence |
|---|---|---|---|
| `T4-AUTH-001` | `PASS` | `frontend/e2e/integration/auth.setup.ts` reached backend health before bootstrap/login work started. | Terminal log `725906.txt` |
| `T4-AUTH-002` | `PASS` | Approved bootstrap path executed in `APP_ENV=development` with `ALLOW_E2E_SUPERUSER_BOOTSTRAP=1`, then logged in over real `/api/auth/login`. | Terminal log `725906.txt` |
| `T4-AUTH-003` | `PASS` | Auth state file was written to `frontend/e2e/integration/.auth/user.json`; git treated it as ignored/untracked rather than committed content. | Terminal log `725906.txt`; cleanup attestation `725907.txt` |
| `T4-AUTH-004` | `PASS` | Production-like classification was rechecked separately: `APP_ENV=staging .venv/bin/python scripts/create_test_user.py --is-superuser` hard-failed with `RuntimeError: create_test_user.py is disabled when APP_ENV is staging or production`. | Follow-up shell output in this session; guard implemented in `backend/scripts/create_test_user.py` |
| `T4-AUTH-005` | `NOT RE-EXECUTED` | Unhealthy-backend negative path was not rerun in this follow-up. The executed setup still used the same fail-closed `/health` polling gate. | `frontend/e2e/integration/auth.setup.ts` |
| `T4-AUTH-006` | `NOT RE-EXECUTED` | Login-failure/corrupt-storage negative path was not rerun in this follow-up. This artifact is limited to successful setup, environment classification, and cleanup traceability. | `frontend/e2e/integration/auth.setup.ts` |
| `T4-AUTH-007` | `PASS` | Post-run cleanup completed: `frontend/e2e/integration/.auth/user.json` was deleted, the local backend process was stopped, and the disposable PostgreSQL container was removed. | Cleanup attestation `725907.txt`; follow-up shell output in this session |

## Sanitized evidence attached

- Redacted auth-setup terminal log: `725906.txt`
- Redacted cleanup attestation: `725907.txt`
- Production-like deny result summarized above without stdout secrets
- No auth cookies, refresh/access tokens, or `.auth/*.json` contents copied into this file

## Redaction rules applied

- No raw password or cookie values are pasted here.
- `create_test_user.py` stdout was not copied because it includes the fixture password by design.
- Only the executed actor label/email, environment classification, exit status, and cleanup handling are recorded.

## Outcome

- Final status: `READY FOR QA RE-REVIEW`
- Blocking issue, if any: none in the scoped follow-up artifact; negative cases `T4-AUTH-005` and `T4-AUTH-006` were not rerun and remain code-traceable only
- Ready to unlock `T4-LIVE-001`: yes, as the pinned setup artifact for the `b75883cbdce230b59abc8b59fae587d51db07a96` evidence package
