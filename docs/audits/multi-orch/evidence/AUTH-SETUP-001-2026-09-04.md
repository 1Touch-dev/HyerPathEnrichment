# `AUTH-SETUP-001` Execution Evidence — 2026-09-04

Status: `PASS`
Execution state: `COMPLETED (LOCAL LIVE SIGN-OFF)`

## Run metadata

- Date: 2026-09-04
- Operator: Cursor agent
- Environment name: shared-lane auth bootstrap rerun
- Environment classification: dedicated local frontend + dedicated local backend on a disposable SQLite DB, host Redis, non-production only
- Executed revision pin: `f39941a011b3df4f7b3ed37aee9ba817eb4637b4`
- Branch: `product-doors/remediation-shared-lane`
- Worktree: `/home/axiz/HyerPathEnrichment/.worktrees/product-doors-remediation-shared-lane`
- Backend URL: `http://127.0.0.1:8010`
- Frontend URL / port: `http://127.0.0.1:4335`
- Approved `DEC-T4-SETUP` option: reuse `backend/scripts/create_test_user.py` plus real HTTP login in a non-production environment only
- Security reviewer sign-off reference: `DEC-T4-SETUP` in `docs/audits/multi-orch/master-decision-register.md`
- DB prep before final run: `APP_ENV=development DATABASE_URL=sqlite+aiosqlite:////tmp/product-doors-signoff.db TEST_DATABASE_URL=sqlite+aiosqlite:////tmp/product-doors-signoff.db .venv/bin/python -m alembic upgrade head`
- Final Playwright command: `APP_ENV=development ALLOW_E2E_SUPERUSER_BOOTSTRAP=1 DATABASE_URL=sqlite+aiosqlite:////tmp/product-doors-signoff.db TEST_DATABASE_URL=sqlite+aiosqlite:////tmp/product-doors-signoff.db BACKEND_API_URL=http://127.0.0.1:8010 BACKEND_API_TOKEN=product-doors-signoff-token FRONTEND_USE_MOCKS=false PLAYWRIGHT_PORT=4335 PLAYWRIGHT_REUSE_SERVER=false INTEGRATION_TEST_EMAIL=e2e-integration@example.com INTEGRATION_TEST_PASSWORD=IntegrationTest123 npx playwright test e2e/integration/auth.setup.ts --project integration-setup`

## Dependency checklist

- [x] `DEC-T4-SETUP` approved
- [x] Non-production environment confirmed
- [x] Dedicated backend `/health` returned `200`
- [x] Disposable DB schema prepared before bootstrap/login
- [x] Approved setup mechanism used without modifying release-surface files
- [x] Cookie-jar file remained gitignored and was cleaned up after validation

## Case ledger

| Case ID | Result | Notes | Evidence |
|---|---|---|---|
| `T4-AUTH-001` | `PASS` | `frontend/e2e/integration/auth.setup.ts` reached backend health before bootstrap/login work started. | Focused auth setup Playwright rerun on `f39941a` |
| `T4-AUTH-002` | `PASS` | Approved bootstrap path executed in `APP_ENV=development` with `ALLOW_E2E_SUPERUSER_BOOTSTRAP=1`, then logged in over real `/api/auth/login`. | Focused auth setup Playwright rerun on `f39941a` |
| `T4-AUTH-003` | `PASS` | Auth state file was written to `frontend/e2e/integration/.auth/user.json` during setup, and git treated the path as ignored/untracked. | Focused auth setup Playwright rerun on `f39941a`; local cleanup attestation |
| `T4-AUTH-004` | `PASS` | Production-like classification was rechecked separately: `APP_ENV=staging ALLOW_E2E_SUPERUSER_BOOTSTRAP=1 .venv/bin/python scripts/create_test_user.py --is-superuser` hard-failed with `RuntimeError: create_test_user.py is disabled when APP_ENV is staging or production`. | Runtime shell verification on `f39941a`; guard implemented in `backend/scripts/create_test_user.py` |
| `T4-AUTH-005` | `NOT RE-EXECUTED` | Unhealthy-backend negative path was not rerun; the executed setup still relied on the same fail-closed `/health` polling gate. | `frontend/e2e/integration/auth.setup.ts` |
| `T4-AUTH-006` | `NOT RE-EXECUTED` | Login-failure/corrupt-storage negative path was not rerun in this sign-off pass. | `frontend/e2e/integration/auth.setup.ts` |
| `T4-AUTH-007` | `PASS` | Post-run cleanup completed: `frontend/e2e/integration/.auth/user.json` was deleted and the path remained gitignored. | Local cleanup attestation (`git check-ignore -v ... && test ! -e ...`) |

## Sanitized evidence attached

- Focused auth-setup Playwright rerun on `f39941a`
- Production-like deny result summarized above without stdout secrets
- Cleanup attestation confirming `.auth/user.json` is ignored and absent after removal
- No auth cookies, refresh/access tokens, or `.auth/*.json` contents copied into this file

## Redaction rules applied

- No raw password or cookie values are pasted here.
- `create_test_user.py` stdout was not copied because it includes the fixture password by design.
- Only the executed actor label/email, environment classification, exit status, and cleanup handling are recorded.

## Outcome

- Final status: `PASS`
- Blocking issue, if any: none in the executed auth setup rows
- Ready to unlock `T4-LIVE-001`: yes, as the pinned setup artifact for the `f39941a011b3df4f7b3ed37aee9ba817eb4637b4` evidence package
