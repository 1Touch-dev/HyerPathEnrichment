"""This module owns no tables of its own — application_status/status_updated_at live on
job_matching's JobMatch, per RULE.md "do not duplicate", same convention job_swipe/models.py
already documents for JobMatch/JobPosting."""

from __future__ import annotations
