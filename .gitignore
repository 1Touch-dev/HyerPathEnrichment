# Dependencies
node_modules/
.venv/
venv/

# Orphan uv lockfile — CI/Docker use pip install -e (not uv sync); do not commit
backend/uv.lock

# Build / output
dist/
build/
.next/
out/
*.tsbuildinfo
coverage/

# Env & secrets
.env
.env.*
!.env.example
backend/.env
frontend/.env.local

# Session cookie jars (never commit; may contain JWTs / refresh tokens)
**/cookies.txt
backend/docker/cookies.txt
backend/cookies.txt

# Logs & OS
*.log
.DS_Store
Thumbs.db
ehthumbs.db
desktop.ini

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.tox/
htmlcov/
.coverage
.coverage.*
.hypothesis/

# Local databases
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# Backend dev/runtime artifacts
backend/artifacts/
.e2e-results/
.asset-cache/

# Playwright e2e auth session state (contains credential material)
frontend/e2e/integration/.auth/

# Local Tier 1 canary profiles (copy from tier1_canary_set.example.json)
backend/docs/tier1_canary_set.json
backend/docs/tier1_retry_set.json

# Local Tier 2–4 canary profiles (copy from tier234_canary_set.example.json)
backend/docs/tier234_canary_set.json

# Frontend extras
.vercel/
.turbo/
.eslintcache
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# Playwright
playwright-report/
test-results/
blob-report/
frontend/e2e/integration/.auth/

# IDE / editor
.vscode/
.idea/
.cursor/

# Agent / local tooling state
.openclaw/
.paperclip/
.zero-human/

# Agent local session notes (not for org repo)
memory/

# Orchestrator-managed git worktrees (kept inside workspace for file-write approval scoping)
.worktrees-m2/
.worktrees/
